{
    "name": "backend",
    "version": "0.0.1",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "node --watch index.js",
        "build": "rm -rf dist/ && cd ../../frontend/lectureCode/ && npm run build && cp -r dist ../../backend/lectureCode/",
        "deploy": "git add . && git commit -m deploying && git push origin main",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Ivan Martynov",
    "license": "MIT",
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^5.1.0"
    }
}
